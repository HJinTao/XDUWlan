# 0003：使用系统凭据库保存密码

## 状态

已接受。

## 背景

自动认证需要保存密码，但普通配置文件和日志不适合保存凭据。

## 决策

通过 `keyring` 使用 macOS、Windows 和 Linux 对应的系统凭据库。密码由交互式 `configure` 写入，认证时读取。

## 理由

系统凭据库比明文文件更适合保存长期密码，也能把平台差异限制在凭据适配器中。

## 后果

某些无桌面环境可能没有可用的凭据后端；程序必须明确报告问题，不得静默回退到明文保存。
