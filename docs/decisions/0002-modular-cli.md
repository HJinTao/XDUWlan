# 0002：采用模块化 CLI 与核心领域模型

## 状态

已接受。

## 背景

项目同时包含网络探测、Portal 认证、自服务监测、凭据和自动重连。脚本集合会让职责和错误处理快速耦合。

## 决策

采用 CLI、应用服务、核心模型与端口接口、基础设施适配器四层结构。Portal 与自服务保持独立。

## 理由

模块可以单独学习和测试，未来桌面界面可以复用应用层，其他语言可以根据同一接口重写。

## 后果

前期需要定义数据模型和接口，代码量略多；但能避免认证、监测和平台代码互相复制。
