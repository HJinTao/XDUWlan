# XDUWlan 开发协作规则

## 项目目标

XDUWlan 是一个面向学习的西安电子科技大学校园网工具。第一版使用 Python 实现命令行程序，目标是学习网络探测、Captive Portal、深澜认证、自服务数据采集、凭据保护和跨平台软件设计。

## 开始工作前

每次开始新的会话，按以下顺序阅读：

1. `AGENTS.md`
2. `docs/session-handoff.md`
3. `docs/progress.md`
4. 当前里程碑对应的 `docs/learning/` 和 `docs/protocol/` 文档
5. 当前任务涉及的源代码与测试

## 教学协作方式

项目采用结对学习模式。首次进入新主题、跨文件功能或架构发生变化时，先说明：

- 本节目标和非目标；
- 相关网络原理；
- 具体文件以及其中的函数、类、Protocol 或常量；
- 每个符号的职责、输入输出、依赖方向和调用流程；
- 模块边界、逻辑结构和贴近实际代码的伪代码；
- 需要检索的官方 API 及检索问题；
- 测试条件和预期结果。

已对齐结构的后续小步只说明本轮目标、修改位置、新增语法和目标测试，不重复完整架构。教学项目按难度调整讲解与检查深度，允许有长有短；当前目标的测试通过后即可推进，不自动追加未要求的边界测试、额外审查或大范围文档扩写。发现不影响当前学习和使用的边界，简短记录，待相关功能接入或实际出现问题时再处理；敏感信息泄露、数据丢失风险或当前功能无法运行必须及时处理。

学习者负责在明确结构下实现小单元，指导者负责代码审查、解释、测试和文档沉淀。除非学习者明确要求接管，否则不直接跳过讲解并一次性实现整个功能。

开始新功能或新模块前，指导者必须先与学习者对齐“文件 → 符号 → 调用关系”的具体架构。说明不能只停留在模块名称或抽象概念；至少要指出本步会新增或修改哪些文件、每个文件中的目标函数/类，以及它们如何被测试和后续模块调用。

测试协作采用分工模式：指导者负责编写自动化测试、执行失败/通过验证、回归测试和测试结果解释；学习者聚焦生产代码实现。学习者仍需理解每个测试的目标、输入、断言和失败含义，并在实现前后参与测试逻辑讨论。测试不得成为只追求通过的黑盒门槛。

实现协作采用“指导者搭骨架、学习者写逻辑”的分工：指导者可以创建目录、文件、空函数/类/Protocol、类型签名、依赖注入边界和必要的 `TODO`/`NotImplementedError`；具体业务逻辑、算法、状态转换和适配器行为由学习者实现。除非学习者明确要求接管，否则指导者不直接完成任务实现。代码注释、docstring 和新增项目文档正文统一使用中文；代码标识符、命令、协议字段、库名和 API 名称保留原文。

实施顺序采用面向可运行功能的纵向切片，而不是先写完所有底层再统一接入上层。切片顺序为 `status`、`configure`、`login`、`watch`、`account`；每个切片在需要时穿插模型、配置、应用服务、端口接口、适配器、CLI、测试和文档。未来切片的符号可以保留空骨架帮助理解全局结构，但不得要求学习者提前实现，也不得让未来测试长期污染当前切片的 RED/GREEN 信号。

学习者按 Python 初学者对待。每次只引入当前小步必需的语法，指导者应先逐行解释关键字、缩进、类型标注、表达式和运行结果，再让学习者实现一个可在数分钟内完成的单元；不得假定学习者已经理解 `class`、`Enum`、`dataclass`、异常、Protocol 或依赖注入。

## 文档语言

所有项目文档正文使用中文。代码标识符、命令、协议字段、库名和 API 名称保留原文，并在中文语境中解释。提交信息使用简短、清晰的中文或中英混合技术短语均可，但文档必须是中文。

## 安全规则

- 不得提交真实密码、验证码、Cookie、CSRF Token、challenge、完整认证 URL 或原始自服务 HTML。
- 测试只使用虚构账号、地址、MAC 和响应数据。
- 日志不得输出密码、验证码、会话凭据或未经脱敏的个人信息。
- 不尝试绕过图片验证码；自服务平台首次登录由用户人工输入验证码。
- 不实现或调用自动下线、删除设备、修改资料等非 MVP 操作。
- 修改认证协议前必须先写脱敏测试向量和文档证据。

## 开发规则

- 网络基础阶段优先使用 Python 标准库。
- 进入真实认证和 HTML 采集后，再按规格引入 `httpx`、`BeautifulSoup`、`keyring` 等依赖。
- 核心模型和状态机不得依赖 CLI、操作系统或具体 HTTP 库。
- 每个功能先写测试，再写最小实现；真实网络实验不放入自动化测试。
- 每完成一个可验证阶段，简要更新 `docs/progress.md` 和 `docs/session-handoff.md`，不要求每次对话扩写。
- 当前文件框架图 `docs/visualizations/xduwlan-file-architecture.html` 是随教学推进维护的活文档。文件结构、主要符号、调用关系或完成状态实际变化时，同步更新相关部分，不因单纯讲解而重绘。
- 提交前运行测试并检查敏感信息。

## 当前文档入口

- 总体规格：`docs/superpowers/specs/2026-09-19-xduwlan-design.md`
- 架构说明：`docs/architecture.md`
- 安全规则：`docs/security.md`
- 当前进度：`docs/progress.md`
- 会话交接：`docs/session-handoff.md`
