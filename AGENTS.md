# XDUWlan 开发协作规则

## 项目目标

XDUWlan 是一个面向学习的西安电子科技大学校园网工具。第一版使用 Python 实现命令行程序，目标是学习网络探测、Captive Portal、深澜认证、自服务数据采集、凭据保护和跨平台软件设计。

## 开始工作前

每次开始新的会话，按以下顺序阅读：

1. `AGENTS.md`
2. `docs/session-handoff.md`
3. `docs/progress.md`
4. 当前里程碑对应的 `docs/learning/` 和 `docs/protocol/` 文档
5. 当前任务涉及的源代码与测试

## 教学协作方式

项目采用结对学习模式。每个小任务都必须先说明：

- 本节目标和非目标；
- 相关网络原理；
- 逻辑结构、模块边界和伪代码；
- 需要检索的官方 API 及检索问题；
- 测试条件和预期结果。

学习者负责在明确结构下实现小单元，指导者负责代码审查、解释、测试和文档沉淀。除非学习者明确要求接管，否则不直接跳过讲解并一次性实现整个功能。

## 文档语言

所有项目文档正文使用中文。代码标识符、命令、协议字段、库名和 API 名称保留原文，并在中文语境中解释。提交信息使用简短、清晰的中文或中英混合技术短语均可，但文档必须是中文。

## 安全规则

- 不得提交真实密码、验证码、Cookie、CSRF Token、challenge、完整认证 URL 或原始自服务 HTML。
- 测试只使用虚构账号、地址、MAC 和响应数据。
- 日志不得输出密码、验证码、会话凭据或未经脱敏的个人信息。
- 不尝试绕过图片验证码；自服务平台首次登录由用户人工输入验证码。
- 不实现或调用自动下线、删除设备、修改资料等非 MVP 操作。
- 修改认证协议前必须先写脱敏测试向量和文档证据。

## 开发规则

- 网络基础阶段优先使用 Python 标准库。
- 进入真实认证和 HTML 采集后，再按规格引入 `httpx`、`BeautifulSoup`、`keyring` 等依赖。
- 核心模型和状态机不得依赖 CLI、操作系统或具体 HTTP 库。
- 每个功能先写测试，再写最小实现；真实网络实验不放入自动化测试。
- 每完成一个可验证小目标，更新 `docs/progress.md` 和 `docs/session-handoff.md`。
- 提交前运行测试并检查敏感信息。

## 当前文档入口

- 总体规格：`docs/superpowers/specs/2026-09-19-xduwlan-design.md`
- 架构说明：`docs/architecture.md`
- 安全规则：`docs/security.md`
- 当前进度：`docs/progress.md`
- 会话交接：`docs/session-handoff.md`
