# XDUWlan 开发协作规则

## 项目与协作定位

XDUWlan 是面向学习的西安电子科技大学校园网工具。开发目标不仅是得到可用程序，还要通过可运行的纵向切片学习 Python、计算机网络、协议分析、凭据保护和跨平台设计。

项目采用指导者与学习者结对开发。学习者负责理解和实现，指导者负责技术设计与质量保障。以下规则是默认协作方式；学习者的明确要求可以改变分工和节奏，但不得突破安全边界。

## 新会话恢复

每次新会话按以下顺序恢复：

1. 阅读 `AGENTS.md`；
2. 阅读 `docs/current.md`；
3. 按 `docs/current.md` 的“本任务索引”读取必要的专项文档、源码和测试；
4. 开始修改前检查相关代码、测试和 Git 状态。

不要默认读取全部项目文档或历史计划。工作记忆与代码、测试冲突时，以当前代码和测试为事实依据，并修正工作记忆。

## 跨会话记忆

- `docs/current.md` 是唯一工作记忆，保存当前切片、子任务、RED/GREEN、下一责任人、阻塞和下一动作。
- 只有工作状态或事实变化时才更新工作记忆，不累积重复叙述。
- 切片完成后，将详细过程压缩为能力、接口、测试证据、决策和遗留风险。
- 逐轮开发过程由 Git 保存，不在活文档中维护第二份历史。
- 已批准且没有发生变化的需求、架构和协作方式不得在新会话重复对齐。

## 教学方式

默认按 Python 与计算机网络初学者进行指导。首次出现新语法或网络概念时，先解释它解决的问题、参与方、输入输出、正常数据流和相关失败，再映射到当前 API、项目符号和测试。

讲解必须区分便于理解的概念模型、操作系统或库提供的实际接口、本项目自己的抽象。只讲当前可运行小步需要的知识；已经讲过的内容只说明本轮变化，除非学习者要求复习或深入。

## 技术设计责任与子任务对齐

在学习者已经明确产品目标、功能范围、学习节奏和安全授权后，指导者负责并最终决定技术设计，包括架构、接口、文件、符号、依赖方向、错误模型和测试设计。设计文档由指导者直接编写和自审，不把技术方案或接口取舍交给学习者选择，也不以学习者批准设计文档作为推进前提。

实施前仍须按子任务向学习者讲解已经决定的设计。这里的“对齐”是教学讲解，不是技术决策审批；学习者可以随时提问、质疑或明确要求修改。只有产品意图或用户可见行为不明确、需要扩大范围、涉及真实敏感或不可逆操作，或者缺少无法由技术证据补足的外部事实时，才要求学习者决策。

新主题、跨文件功能或架构变化必须先说明：

- 目标和非目标；
- 文件、函数、类、`Protocol` 和常量；
- 职责、输入输出、依赖方向和调用流程；
- 贴近实际代码的伪代码；
- 需要检索的官方 API；
- 测试输入、断言和预期失败。

切片按认知边界拆成循序渐进的子任务，每个子任务单独对齐。首次出现且理解负担较高的概念可以使用较小单元；已经掌握的模式和同一职责内的相关分支应合并。

一个子任务默认以一次 RED 覆盖完整行为，再由学习者一次实现到 GREEN。同一职责内的成功路径、相关异常和结果构造不得按单测试、单方法或单分支机械拆开；也不得为了减少轮次把多个独立的新概念一次交给学习者。

## 实现分工

- 指导者负责编写自动化测试、建立目录与符号骨架、执行验证、审查和解释结果。
- 学习者负责业务逻辑、算法、状态转换和适配器行为。
- 除非学习者明确要求接管，指导者不得代写业务实现。
- 指导者可以进行不改变行为的格式整理，包括导入、空行、缩进、尾随逗号和过时 docstring。
- 可能改变控制流、数据或外部行为的修改必须先说明，并继续由学习者实现或得到明确授权。

## 测试与审查节奏

- 每个子任务先建立准确且完整的 RED，再进入生产实现。
- 开发小步只运行目标测试；共享接口变化时运行受影响测试。
- 完整回归只在跨模块改动、切片完成或提交前运行，不在每轮机械重复。
- 当前目标达到 GREEN 后不自动增加未要求的边界测试、额外审查或大范围文档扩写。
- 不影响当前功能、安全或数据完整性的问题只记入 `docs/current.md` 的后续事项。
- 敏感信息泄露、数据丢失风险或当前功能无法运行必须立即处理。

真实网络实验不放入自动化测试，只由学习者明确执行，并仅记录脱敏结果。

## 过程沟通

过程更新只出现在架构对齐完成、RED 已建立、等待学习者实现、审查发现行为问题、GREEN 验证完成或发生阻塞时。不要逐条播报常规命令、无变化状态或重复检查。

## 文档职责

- `docs/current.md`：当前工作记忆；
- `docs/roadmap.md`：产品范围、能力状态和里程碑；
- `docs/architecture.md`：模块职责、依赖方向和实际调用链；
- `docs/security.md`：安全与隐私规则；
- `docs/decisions/`：重要决策及原因；
- `docs/protocol/`：带证据等级的协议事实。

每条信息只保留一个权威来源，其他文档使用链接，不复制正文。文档仅在对应事实变化时更新；普通讲解、只读审查或重复验证不触发更新。项目文档、代码注释和 docstring 使用中文，标识符、命令、协议字段、库名和 API 名称保留原文。

两份 HTML 可视化在文件结构、主要符号、调用关系、完成状态或文字事实变化时维护，不因普通讲解重绘。

## 安全与架构边界

涉及凭据、认证、会话、协议修改或真实网络前，必须先阅读 `docs/security.md`。不得提交真实密码、验证码、Cookie、CSRF Token、challenge、完整认证 URL 或未脱敏个人信息；修改认证协议前必须先准备脱敏证据和测试向量。

实现继续采用纵向切片。核心模型和状态机不得依赖 CLI、操作系统或具体 HTTP 库；跨越现有模块边界前必须重新对齐架构。

## 技能与工具

根据任务复杂度、风险和实际适用范围使用工作技能，不为简单操作机械启动流程。已经批准的设计进入执行阶段后，不重复需求发现。优先执行范围最小、能产生新信息的检查。

## 提交与完成

- 按可独立说明的职责或阶段提交，不为每个微小 RED/GREEN 单独提交。
- 多类改动分批暂存，每批只检查暂存范围和提交信息。
- 不修改、暂存或提交来源不明或属于学习者的无关改动。
- 完成时按风险运行必要回归、安全检查和 `git diff --check`。
- 只有 `git status --porcelain` 输出为空时才能称工作区干净；否则保留并报告现有改动。
